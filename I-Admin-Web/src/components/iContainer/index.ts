import IContainer from './i-container'


/**
 * IContainer容器组件
 * <p>特点:
 * <p>1. el-scrollbar + el-space，带滚动带间距的垂直布局容器
 * <p>2. jsx形式，避免el-space误处理注释导致其占据空间
 * <p>Slots:
 * <p>1. default - 内容区
 */
export default IContainer
